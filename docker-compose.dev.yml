# ─────────────────────────────────────────────────────────────────────────────
#  DESARROLLO LOCAL — Rosales y Asociados · Sistema de Expedientes
#
#  Laravel y Vite corren en local (no en Docker):
#    cd expedientes-app
#    php artisan serve        → http://localhost:8000
#    npm run dev              → HMR en http://localhost:5173
#    php artisan queue:work   → (solo si pruebas extracción de docs)
#
#  Levantar infraestructura base:
#    docker compose -f docker-compose.dev.yml up -d
#
#  Levantar con servicio IA:
#    docker compose -f docker-compose.dev.yml --profile ai up -d
#
#  Build del servicio IA (solo cuando cambia requirements.txt o Dockerfile):
#    docker compose -f docker-compose.dev.yml build ai
# ─────────────────────────────────────────────────────────────────────────────

services:

  # ─── PostgreSQL 16 + pgvector ─────────────────────────────────────────────────
  postgres:
    image: pgvector/pgvector:pg16
    container_name: expedientes_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: expedientes_juridicos
      POSTGRES_USER: laravel
      POSTGRES_PASSWORD: secret
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - juridico_net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U laravel -d expedientes_juridicos"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ─── Redis ───────────────────────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: expedientes_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - juridico_net

  # ─── FastAPI — Servicio IA (solo con --profile ai) ───────────────────────────
  ai:
    profiles: ["ai"]
    build:
      context: ../expedientes-ai
      dockerfile: Dockerfile
    container_name: expedientes_ai
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql://${DB_USERNAME:-laravel}:${DB_PASSWORD:-secret}@postgres:5432/${DB_DATABASE:-expedientes_juridicos}
      STORAGE_PATH: /storage/documentos
      LLM_PROVIDER: ${LLM_PROVIDER:-anthropic}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      LLM_BASE_URL: ${LLM_BASE_URL:-}
      LLM_MODEL: ${LLM_MODEL:-claude-sonnet-4-6}
      TRANSFORMERS_CACHE: /app/.cache/huggingface
    volumes:
      - ../expedientes-app/storage/app/documentos:/storage/documentos:ro
      - ../expedientes-ai/app:/app/app
      - ../expedientes-ai/scripts:/app/scripts
      - huggingface_cache:/app/.cache/huggingface
      - ../expedientes-app/recursos/leyes:/app/recursos/leyes:ro
      - ../expedientes-ai/recursos/memoriales:/app/recursos/memoriales:ro
    ports:
      - "8001:8000"
    networks:
      - juridico_net
    depends_on:
      postgres:
        condition: service_healthy

  # ─── PostgreSQL para TSJ Jurisprudencia (solo con --profile tsj) ────────────
  tsj_postgres:
    profiles: ["tsj"]
    image: pgvector/pgvector:pg16
    container_name: tsj_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: tsj
      POSTGRES_PASSWORD: tsj
      POSTGRES_DB: tsj_jurisprudencia
    ports:
      - "5433:5432"
    volumes:
      - tsj_postgres_data:/var/lib/postgresql/data
    networks:
      - juridico_net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U tsj -d tsj_jurisprudencia"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ─── TSJ Jurisprudencia — búsqueda semántica de resoluciones TSJ Bolivia ─────
  tsj:
    profiles: ["tsj"]
    build:
      context: ../tsj_jurisprudencia
      dockerfile: Dockerfile
    container_name: tsj_jurisprudencia
    restart: unless-stopped
    env_file:
      - ../tsj_jurisprudencia/.env          # lee toda la config del .env del proyecto tsj
    environment:
      DATABASE_URL: postgresql+psycopg://tsj:tsj@tsj_postgres:5432/tsj_jurisprudencia
    volumes:
      - ../tsj_jurisprudencia/tsj_jurisprudencia:/app/tsj_jurisprudencia
    ports:
      - "8002:8000"
    networks:
      - juridico_net
    depends_on:
      tsj_postgres:
        condition: service_healthy

networks:
  juridico_net:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  huggingface_cache:  # modelo multilingual-e5-large (~1.2GB) — persiste entre rebuilds
  tsj_postgres_data:  # resoluciones TSJ indexadas — persiste entre rebuilds
